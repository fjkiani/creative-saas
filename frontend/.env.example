# Supabase — get these from your Supabase project dashboard
VITE_SUPABASE_URL=https://cllqahmtyvdcbyyrouxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Backend API URL — leave empty for same-origin (nginx proxy handles /api/ in production)
# For local dev, set to your backend URL:
VITE_API_URL=http://localhost:8000

# API key — must match PIPELINE_API_KEY on the backend (leave empty if auth disabled)
VITE_API_KEY=
